overwrite: true
schema: 'http://127.0.0.1:3000/graphql'
documents: null
generates:
  graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      useIndexSignature: true
